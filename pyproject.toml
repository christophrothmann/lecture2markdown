[project]
name = "lecture2markdown"
version = "0.1.0"
description = "Konvertiert PDF-Vorlesungsfolien in Markdown mithilfe der OpenAI Vision API."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.52.0",
    "pymupdf>=1.28.0",
    "pillow>=10.0.0",
    "python-dotenv>=1.0.0",
    "tqdm>=4.66.0",
    "tenacity>=9.1.4",
]

[tool.uv]
package = false
